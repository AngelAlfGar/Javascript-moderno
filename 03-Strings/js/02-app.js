const producto = 'Monitor de 20 pulgadas';

console.log(producto);

// metodo para conocer la cantidad de caracteres de la cadena
console.log(producto.length);

/* bosca si esxiste el dato requerido dentro de la cadena 
y te devuelve la posicion en la que empieza */
console.log(producto.indexOf('200'));

/* busca si existe el dato requerido dentro de la cadena 
y te devuelve un true o un false */
console.log(producto.includes('200'));
console.log(producto.includes('Monitor'));


